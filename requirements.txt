# CPU-only core (chạy được ngay trên máy thường)
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
xgboost>=2.0
rapidfuzz>=3.0
scipy>=1.10
joblib>=1.3

# Tuỳ chọn (chỉ cần khi chạy nhánh embedding mức dòng trên Colab/GPU)
# torch
# transformers
# sentencepiece
